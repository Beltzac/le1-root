# LE1 fixes — 2026-09-03 (LE1 offline, all tests local)

LE1 reachable test: `ssh u0_a50@100.124.251.81:8022` → timeout (car off). No on-device run. Everything below was tested on the Termux host instead.

## Tests run (host)
- `sh -n` on all `post-root/*.sh` + `*.sh` → all OK (before and after).
- `clang -fsyntax-only -Wall exploit/le1_root.c` → clean.
- Manifest round-trip (`record` → `restore.sh` parse) → format OK, but restore mishandled `<unset>`.
- NMEA GPRMC parse → **Y2K bug**: `230394` parsed as `2094` (hardcoded `20` prefix).
- `/data/local/tmp` writability → `drwxrwx--x shell:shell`, `touch` as app uid **fails** → deploy log path broken pre-root. Same perms expected on LE1 for u0_a50.
- `grep` audit: hardcoded `mmcblk0p22`, bare `busybox/curl/ping` (init PATH-minimal), `mount -o rw,remount /system` single syntax, backups to /system (90% full), README/deploy vs exploit daemon-model contradiction, POST-ROOT-PLAN backup ordering, LE1_ROOT_KERNEL `adb root now works` claim.

## Fixes applied
1. `post-root/gpstime.sh` — GPRMC year windowing: 80–99→19yy else 20yy (GPS rollover safe). Verified: 230394→1994, 150826→2026.
2. `post-root/restore.sh` — fixed log-var bug (`$a1`→`$_a1`) + `<unset>`/empty now clears the prop instead of setting literal `<unset>`.
3. `post-root/common.sh` — `require_root` falls back to `whoami`; `sys_rw/sys_ro` try `/system /system` then legacy syntax; `backup()` now writes to `/data/misc/le1-time/backups` (not /system).
4. `post-root/timefix.sh` — absolute `BB`/`CURL` discovery (`/system/xbin` vs `/system/bin` vs PATH) for init-service context; NTP/HTTP/hwclock use `$BB`/`$CURL`.
5. `post-root/loadtime.sh` — removed false toybox `date -d @epoch` fallback (Android 8.1 toybox lacks it); now fails loudly demanding busybox.
6. `deploy.sh` + `README.md` — exploit log moved to `~/le1_root.log` (u0_a50-writable) with reason documented; README Next-steps updated to daemon model (0755 sudaemon+su client, no setuid 6755).
7. `exploit/le1_root.c` — `/system` remount auto-detects device from `/proc/mounts`, tries NULL-source remount + explicit dev + shell fallbacks (no more hardcoded mmcblk0p22); sudaemon source falls back to `/sdcard/sudaemon`.
8. `backup-emmc.sh` — documented pre-root gap (needs su → post-root only; pre-root = mtkclient/SPFT readback) + must run before any /system write.
9. `POST-ROOT-PLAN.md` — safety net: backup-first ordering + Oct-2025 vs Aug-2020 build-mismatch warning.
10. `kernel_3.18.79/LE1_ROOT_KERNEL.md` — §4d corrected: permissive kernel alone ≠ `adb root`; ramdisk `ro.secure=0`/`ro.debuggable=1` also required.

## Still needs LE1 online (could not test)
- Re-run `./deploy.sh` (compile + exploit + `~/le1_root.log` tail).
- `leak_debug2.c` if phase1 leaks non-kernel ptr.
- On-device verify: busybox applets, NMEA `nc 127.0.0.1 7000`, DuraSpeed pkg name, `pm list packages`, `/proc/mounts` /system device, RTC persistence, `backup-emmc.sh critical` as first root command.
- Revert `persist.adb.tcp.port` cleanup (STATUS.md) when next online.
